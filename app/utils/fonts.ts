import { Megrim, Zen_Kurenaido } from 'next/font/google';

export const megrim = Megrim({
  subsets: ['latin'],
  weight: ['400'],
});
export const zenKurenaido = Zen_Kurenaido({
  subsets: ['latin'],
  weight: ['400'],
});
